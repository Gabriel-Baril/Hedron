include "core.fbs";

namespace hdn;

enum CPlatform : byte
{
    UNKNOWN,
    PC,
}

table XBuildConfigAsset {
    id: ulong;
    name: string;
    platform: CPlatform;
    features: [string];
}

root_type XBuildConfigAsset;
