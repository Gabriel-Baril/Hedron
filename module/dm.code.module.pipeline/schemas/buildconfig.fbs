include "core.fbs";

namespace dm;

enum CPlatform : byte
{
    UNKNOWN,
    PC,
}

table XBuildConfigAsset {
    id: ulong;
    name: string;
    platform: CPlatform;
    features: [string];
}

root_type XBuildConfigAsset;
